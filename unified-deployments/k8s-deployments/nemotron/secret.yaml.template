# HuggingFace Token Secret for model download authentication
# 
# IMPORTANT: Copy this file to secret.yaml and replace <YOUR_HF_TOKEN> with your actual token
#
# To create your token:
#   1. Go to https://huggingface.co/settings/tokens
#   2. Create a new token with "Read" access
#   3. Accept the model license at: https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Nano-30B-A3B-FP8
#
# Alternative: Create secret directly via kubectl:
#   kubectl create secret generic hf-token-secret \
#     --namespace=llm-inference \
#     --from-literal=HF_TOKEN=hf_xxxxxxxxxxxxx
#
apiVersion: v1
kind: Secret
metadata:
  name: hf-token-secret
  namespace: llm-inference
  labels:
    app.kubernetes.io/name: nemotron-vllm
    app.kubernetes.io/component: credentials
type: Opaque
stringData:
  HF_TOKEN: "<YOUR_HF_TOKEN>"


